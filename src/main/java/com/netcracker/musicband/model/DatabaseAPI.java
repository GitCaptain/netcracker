/*
package com.netcracker.musicband.model;

import com.netcracker.musicband.domain.Group;
import com.netcracker.musicband.domain.Key;
import com.netcracker.musicband.domain.Track;
import com.netcracker.musicband.domain.User;
import com.netcracker.musicband.domain.Instrument;
import com.netcracker.musicband.domain.SoundSample;

public interface DatabaseAPI {

    void createDatabase();

    User[] getAllUsers();

    Group[] getAllGroups();

    Track[] getAllTrack();

    Key[] getAllKeys();

    Instrument[] getAllInstruments();

    SoundSample[] getAllSamples();

    User getUser(int id);

    Group getGroup(int id);

    Track getTrack(int id);

    Instrument getInstrument(int id);

    void addUser(User user);

    void addGroup(Group group);

    void addTrack(Track track);

    void addInstrument(Instrument instrument);

    void createTable();

    void fillTable();
}
*/